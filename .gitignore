# Rate-limit lock files (ephemeral, no value in git)
data/comments/_rate/

# Click tracking (user data, regenerated on server)
data/clicks.json

# Local dev / system files
.DS_Store
Thumbs.db
*.log

# Secrets — never commit, upload manually via FTP after each edit
config/secrets.php
